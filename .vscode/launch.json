{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Django Attach",
      "type": "python",
      "request": "attach",
      "connect": {
        "host": "localhost",
        "port": 9999
      },
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}",
          "remoteRoot": "/app"
        }
      ]
    }
  ]
}
